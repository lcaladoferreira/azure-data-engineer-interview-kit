# SQL Cheatsheet 3

Fast recall sheet for interviews and daily engineering work.


| Need | Use | Watch out for |
|---|---|---|
| Relational foundations | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Query execution order | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Joins and set operations | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Aggregations | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Window functions | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| CTEs and recursion | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Indexes and statistics | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Execution plans | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Transactions and isolation | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
| Warehouse SQL patterns | `SELECT / WITH / OVER` | Check grain, NULLs and deterministic order |
