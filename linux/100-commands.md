# 100 Linux Commands for Data Engineers

Commands grouped around files, pipelines, networking, processes and operations.



## 1. `pwd`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 2. `ls -lah`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 3. `find /data -type f`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 4. `du -sh /data`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 5. `df -h`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 6. `stat file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 7. `file file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 8. `head -n 20 file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 9. `tail -f log`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 10. `wc -l file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 11. `sort file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 12. `uniq -c`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 13. `cut -d, -f1`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 14. `paste a b`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 15. `tr a-z A-Z`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 16. `sed -n '1,20p'`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 17. `awk -F, '{print $1}'`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 18. `grep -R pattern .`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 19. `rg pattern`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 20. `xargs -n1`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 21. `tee output`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 22. `split -l 1000 file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 23. `shuf file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 24. `diff -u a b`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 25. `cmp a b`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 26. `md5sum file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 27. `sha256sum file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 28. `gzip file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 29. `gunzip file.gz`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 30. `tar -czf out.tgz dir`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 31. `tar -xzf out.tgz`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 32. `zip -r out.zip dir`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 33. `unzip out.zip`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 34. `curl -I URL`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 35. `curl -fsSL URL`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 36. `wget URL`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 37. `ssh host`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 38. `scp file host:/tmp`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 39. `rsync -av src/ dst/`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 40. `ip addr`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 41. `ip route`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 42. `ss -tulpn`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 43. `ping -c 4 host`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 44. `dig host`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 45. `nslookup host`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 46. `nc -vz host 443`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 47. `openssl s_client -connect host:443`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 48. `ps aux`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 49. `top`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 50. `free -h`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 51. `uptime`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 52. `lsof file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 53. `lsof -i :443`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 54. `kill PID`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 55. `pkill process`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 56. `nice -n 10 cmd`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 57. `nohup cmd`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 58. `jobs`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 59. `bg`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 60. `fg`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 61. `systemctl status svc`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 62. `journalctl -u svc`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 63. `dmesg`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 64. `crontab -l`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 65. `date -u`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 66. `timedatectl`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 67. `env`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 68. `printenv`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 69. `export KEY=value`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 70. `which python`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 71. `type command`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 72. `history`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 73. `alias`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 74. `chmod 640 file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 75. `chown user:group file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 76. `umask 027`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 77. `id`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 78. `whoami`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 79. `groups`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 80. `sudo -l`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 81. `ln -s source link`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 82. `mkdir -p path`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 83. `touch file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 84. `cp -a src dst`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 85. `mv src dst`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 86. `rm file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 87. `basename path`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 88. `dirname path`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 89. `realpath path`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 90. `readlink link`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 91. `test -f file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 92. `jq '.key' file.json`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 93. `yq '.key' file.yaml`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 94. `python -m json.tool file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 95. `column -t -s, file`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 96. `watch -n 2 cmd`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 97. `time cmd`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 98. `timeout 60 cmd`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 99. `ulimit -a`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.

## 100. `strace -p PID`

Use this command to inspect or operate a data-engineering workload. Run destructive variants only after resolving the exact target; capture exit status and stderr in automation.
